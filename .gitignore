claude.md
gemini.md
todo.md
kitty_output.log
resumeClaude.sh
result
docs/.obsidian/

# The Bearing coordination file (ephemeral session state)
BEARING.md

# Host SSH private keys — never commit these
# Public keys (.pub) are safe to commit and are age recipients for agenix
hosts/**/ssh_host_ed25519_key
hosts/**/ssh_host_rsa_key
BEARING.md
